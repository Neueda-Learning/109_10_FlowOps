package payment.processing.system.Model.Enums;

/**
 * Current employment state of an employee. Only ACTIVE employees are
 * eligible for salary payment processing.
 */
public enum EmploymentStatus {
    ACTIVE,
    ON_LEAVE,
    SUSPENDED,
    TERMINATED
}

