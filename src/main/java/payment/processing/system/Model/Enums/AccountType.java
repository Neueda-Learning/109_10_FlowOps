package payment.processing.system.Model.Enums;

/**
 * Type of bank account. BUSINESS accounts belong to the company (used as
 * payment senders); PERSONAL accounts belong to individual receivers (employees).
 */
public enum AccountType {
    BUSINESS,
    PERSONAL
}

